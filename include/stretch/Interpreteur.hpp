


class Interpreteur
{
    public:
        void gerer_boucle_nombre(const std::string& in) {
            // memoire
        }

    private:
        std::vector<int> memoire;

        // std::map<std::type_info, std::function<void(std::string)>> callbacks; ///< Callback associé à une grammaire donnée
        // callbacks[boucle_nombre]  
}